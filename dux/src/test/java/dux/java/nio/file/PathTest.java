package dux.java.nio.file;

import java.nio.file.Path;

import org.junit.Test;

import com.github.docteurdux.test.AbstractTest;
import com.github.docteurdux.test.Topic;

@Topic(Path.class)
public class PathTest extends AbstractTest {
	@Test
	public void test() {
		/*-
		compareTo(Path)
		endsWith(String)
		endsWith(Path)
		equals(Object)
		getFileName()
		getFileSystem()
		getName(int)
		getNameCount()
		getParent()
		getRoot()
		hashCode()
		isAbsolute()
		iterator()
		normalize()
		register(WatchService, Kind<?>...)
		register(WatchService, Kind<?>[], Modifier...)
		relativize(Path)
		resolve(String)
		resolve(Path)
		resolveSibling(String)
		resolveSibling(Path)
		startsWith(String)
		startsWith(Path)
		subpath(int, int)
		toAbsolutePath()
		toFile()
		toRealPath(LinkOption...)
		toString()
		toUri()
		 */
	}
}
